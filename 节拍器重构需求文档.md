请基于当前 Flutter 节拍器项目进行第二轮优化。

1. 底部导航改为：
   - 左：吉他社 WebView
   - 中：节拍器首页，默认选中
   - 右：设置

2. 吉他社 WebView 地址为：
   https://www.jitashe.org/
   并在 WebView 页面右下角添加悬浮节拍器 Start/Stop 按钮。

3. 首页轻重拍编辑条：

- 去掉上方进度灯。
- 保留四个级别：
  - Accent：重拍
  - Secondary：次重拍
  - Light：轻拍
  - Rest：空拍 / 静音拍

- 点击循环顺序：
  Light → Secondary → Accent → Rest → Light

- 编辑条内显示拍数：
  1、2、3、4、5、6...

- 用颜色和高度表示不同拍型：
  - Accent：最高柱，橙色
  - Secondary：中高柱，蓝色
  - Light：低柱，青绿色
  - Rest：最低/空柱，灰色或暗色

- Rest 空拍必须完全不发声。
  当前问题是 Rest 仍然会播放声音，需要修复。
  播放逻辑中如果 beat.type == BeatType.rest，则跳过音频播放。

- 删除底部 BEAT 计数条。

- 支持长按某一拍弹出单拍编辑面板。
  第一版可以先做 UI 预留。

- 长按弹窗内容建议：
  第 N 拍设置
  当前类型：Accent / Secondary / Light / Rest
  音符时值 / 细分：预留
  确认按钮

4. 修复 Start 区域被底部导航裁剪的问题：
   - 使用 SafeArea
   - 增加底部 padding
   - 优化高度布局
   - 删除 BEAT 卡片后让 Start 按钮完整显示。

5. 拍号抽屉改为双滚轮：
   - 左滚轮：拍数 1~16
   - 右滚轮：时值 1/2/4/8/16/32
   - 下方提供快捷拍型：2/4、3/4、4/4、5/4、6/8
   - 确认后应用到首页，并重新生成 beat 配置。

6. 音色抽屉暂时不要继续开发真实音源逻辑，只保留 UI 入口。

7. 调音器抽屉不要展示假数据。
   如果没有真实麦克风识别逻辑，显示“调音器开发中”。

8. 定时抽屉：
   - 修复 Off / Countdown 按钮组选中后尺寸变化的问题
   - 使用固定宽度 segmented control
   - Apply 后真正启动倒计时
   - 首页定时按钮显示剩余时间
   - 倒计时结束后停止节拍器。

9. 新增保存当前配置功能：
   - 保存 BPM、拍号、轻重拍配置、单拍细分、定时、音色
   - 支持输入配置名称
   - 支持查看、恢复已保存配置
   - 使用本地持久化保存。

10. 播放状态需要全局统一管理：
   - 首页 Start 按钮
   - WebView 悬浮按钮
   - 定时器自动停止
   都需要同步同一个播放状态。